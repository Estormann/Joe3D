<Window x:Class="Joe3D.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Joe3D.WPF"
        xmlns:viewer="clr-namespace:Joe3D.ViewControl;assembly=Joe3D.ViewControl"
        xmlns:helix="clr-namespace:HelixToolkit.Wpf;assembly=HelixToolkit.Wpf"
        DataContext="{Binding Main, Source={StaticResource Locator}}"
        Title="{Binding Title}" Height="350" Width="525">
    <Window.Resources>
    </Window.Resources>
    <DockPanel LastChildFill="True">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Test..." Command="{Binding SetAlpha}"/>
                <MenuItem Header="_Open..." Command="{Binding FileOpenCommand}"/>
                <Separator/>
                <MenuItem Header="E_xit" Command="{Binding FileExitCommand}"/>
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="_Zoom extents" Command="{Binding ViewZoomExtentsCommand}"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_About" Command="{Binding HelpAboutCommand}"/>
            </MenuItem>
        </Menu>
        <!--<helix:HelixViewport3D x:Name="view1" CameraRotationMode="Trackball" ModelUpDirection="0,0,0">
            <ModelVisual3D x:Name="root1">
                <helix:DefaultLights/>
                <ModelVisual3D Content="{Binding CurrentModel}"/>
            </ModelVisual3D>
        </helix:HelixViewport3D>-->
   
        <WrapPanel Orientation="Vertical">
            <WrapPanel Orientation="Horizontal">
            <Label Width="50">Alpha</Label>
            <TextBox Width="50" x:Name="txtAlpha" Text="{Binding Alpha, Mode=TwoWay}" ></TextBox>
            </WrapPanel>
            <WrapPanel Orientation="Horizontal">
            <Label Width="50">Beta</Label>
                <TextBox Width="50" x:Name="txtBeta" Text="{Binding Beta, Mode=TwoWay}"></TextBox>
            </WrapPanel>
            <WrapPanel Orientation="Horizontal">
                <Label Width="50">Theta</Label>
                <TextBox Width="50" x:Name="txtTheta" Text="{Binding Theta, Mode=TwoWay}"></TextBox>
            </WrapPanel>

        </WrapPanel>
        <WrapPanel Orientation="Vertical">
            <WrapPanel Orientation="Horizontal">
                <Label Width="50">Alpha</Label>
                <TextBox Width="50" x:Name="txtAlpha2" Text="{Binding Alpha, Mode=TwoWay}" ></TextBox>
            </WrapPanel>
            <WrapPanel Orientation="Horizontal">
                <Label Width="50">Beta</Label>
                <TextBox Width="50" x:Name="txtBeta2" Text="{Binding Beta, Mode=TwoWay}"></TextBox>
            </WrapPanel>
            <WrapPanel Orientation="Horizontal">
                <Label Width="50">Theta</Label>
                <TextBox Width="50" x:Name="txtTheta2" Text="{Binding Theta, Mode=TwoWay}"></TextBox>
            </WrapPanel>

        </WrapPanel>
        <viewer:Viewer Alpha="{Binding Alpha, Mode=TwoWay}" Beta="{Binding Beta}" Theta="{Binding Theta}">
            
        </viewer:Viewer>
    </DockPanel>
</Window>
